// The global variable
let fixedValue = 4;

function incrementer() {
    // Only change code below this line
    const copyFixedValue = fixedValue
    return copyFixedValue + 1
    // Only change code above this line
}